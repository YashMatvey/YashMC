using System;
            int b = 0;
            int a = 1;
            while(a%2 != 0)
            {
                b += a;
                a = Convert.ToInt32(Console.ReadLine());
            }
            Console.WriteLine($"Сумма нечетных чисел: {b - 1}");
            Console.ReadLine();
